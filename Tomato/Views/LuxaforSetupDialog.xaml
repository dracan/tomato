<Window x:Class="Tomato.Views.LuxaforSetupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Luxafor Setup"
        Width="450"
        Height="280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="None"
        AllowsTransparency="True">

    <Border CornerRadius="8"
            Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource SurfaceBrush}"
            BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="Configure Luxafor Integration"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,8"/>

            <!-- Description -->
            <TextBlock Grid.Row="1"
                       Text="Enter your Luxafor Webhook User ID to enable automatic LED control during focus sessions."
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12"
                       TextWrapping="Wrap"
                       Margin="0,0,0,16"/>

            <!-- User ID Label -->
            <TextBlock Grid.Row="2"
                       Text="Webhook User ID"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       FontSize="14"
                       Margin="0,0,0,4"/>

            <!-- User ID TextBox -->
            <TextBox Grid.Row="3"
                     x:Name="UserIdTextBox"
                     Background="{StaticResource SurfaceBrush}"
                     Foreground="{StaticResource TextPrimaryBrush}"
                     BorderBrush="{StaticResource TextSecondaryBrush}"
                     BorderThickness="1"
                     Padding="8"
                     FontSize="14"
                     FontFamily="Consolas"
                     Margin="0,0,0,8"/>

            <!-- Status and Test Button -->
            <Grid Grid.Row="4" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="StatusText"
                           Grid.Column="0"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        x:Name="TestButton"
                        Content="Test Connection"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Padding="12,6"
                        Margin="0"
                        Click="OnTestConnection"/>
            </Grid>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="5"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="Cancel"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Padding="16,8"
                        Margin="0,0,8,0"
                        Click="OnCancel"/>
                <Button x:Name="SaveButton"
                        Content="Save"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Padding="16,8"
                        IsDefault="True"
                        Click="OnSave"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
